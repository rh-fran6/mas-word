---
# Molecule test dependencies
# These extend the main requirements.yml with test-specific collections.
# The primary collections (kubernetes.core, redhat.openshift, etc.) are
# inherited from the project-level requirements.yml.
collections:
  - name: community.general
    version: ">=8.0.0,<9.0.0"
